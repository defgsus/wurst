import React, { Component } from "react";


const Voice = (props) => {
    const { voice_id } = this.props;

    return (
        <div className={"voice"}>
            <input type={"number"} value={1}/>
        </div>
    );

};

export default Voice;
